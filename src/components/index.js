import React from "react";
import ReactDOM from "react-dom";
import App from "./App"; // No change needed here as App is in the same folder

ReactDOM.render(<App />, document.getElementById("root"));
